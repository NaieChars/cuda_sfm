# sfm 流程

【模块】SFM 主流程（两视图重建）
- **文件**：`SFM.cpp` / `SFM.h`
- **职责**：串联双视图 SfM 完整管线，从图像特征提取到稀疏点云生成，最终输出包含彩色点的重建结果。
- **核心接口**：
  - `SFMResult runSFM(const CameraIntrinsics& intr)`
  - **输入**：`intr` —— 相机内参（来自 `CameraIntrinsics.h`，包含焦距、主点、畸变系数等）。
  - **输出**：`SFMResult` —— 结构体（定义于 `SFMTypes.h`），内部包含 `std::vector<ColoredPoint3D> pointCloud`，每个点含 `x,y,z,r,g,b`。
- **数据流（逐步追踪）**：
  1. **特征提取**：`extractFeaturesFromImages()`（未在本文件定义，推测从固定路径读取图像）→ 返回 `std::vector<FeatureSet> features`，索引 0、1 对应左右图。来源：`FeatureExtractor.h/cpp`。
  2. **GPU 特征匹配**：`cuda_FeatureMatch(features[0], features[1])` → 产出 `std::vector<cuda_MatchResult> matchResult`。来源：`FeatureMatch.cuh/.cu`。匹配结果包含左图特征索引与右图特征索引的对应关系。
  3. **坐标转换**：`convertMatches(features[0], features[1], matchResult, points1, points2)` → 将匹配关系转为两幅图像中 **匹配点对的像素坐标**（`std::vector<cv::Point2f>`），`points1` 对应左图，`points2` 对应右图。来源：`util.h/cpp`。
  4. **本质矩阵估计**：`estimateEssentialMatrix(points1, points2, intr, essentialMask)` → 输出 `cv::Mat E`（3x3 本质矩阵），同时通过 `essentialMask`（`std::vector<uchar>`，大小等于匹配点对数）标记满足极线几何约束的内点（非零为内点）。来源：`EssentialMatrix.h/cpp`。
  5. **姿态恢复**：`recoverCameraPose(E, points1, points2, intr, essentialMask, R, t, inlierCount, poseMask)` → 从本质矩阵分解出 **旋转矩阵 `R`（cv::Mat，3x3）** 和 **平移向量 `t`（cv::Mat，3x1）**，`poseMask` 输出姿态解算后进一步筛选出的内点掩码（非零为有效点）。来源：`PoseRecovery.h/cpp`。
  6. **双重内点筛选（关键）**：
     - 第一次：`fliterInlierPoints(points1, essentialMask)` 和 `fliterInlierPoints(points2, essentialMask)` → 用本质矩阵掩码剔除极线外点，得到 `inlierPoints1/2`。来源：`util.h/cpp`。
     - 第二次：`fliterInlierPoints(inlierPoints1, poseMask)` 和 `fliterInlierPoints(inlierPoints2, poseMask)` → 用姿态恢复掩码再次剔除，得到 `poseInlierPoints1/2`。注意：`poseMask` 长度与原 `essentialMask` 长度一致，此处筛选时需确保掩码索引与点集对齐（本实现中 `essentialMask` 和 `poseMask` 长度相同且对应同一组原始点）。
  7. **GPU 三角化**：`cudaTriangulation(poseInlierPoints1, poseInlierPoints2, intr, R, t, Mask3D)` → 输入经过两次筛选的内点对、内参、外参，输出 `std::vector<cv::Point3f> Points3D`（世界坐标系下的 3D 点），`Mask3D`（`std::vector<int>`）标记三角化是否成功（非零为成功）。来源：`Triangulation.cuh/.cu`。
  8. **点云封装**：遍历 `Points3D`，每个点赋予固定白色（`r=g=b=255`），构造 `ColoredPoint3D` 对象存入 `result.pointCloud`，最终返回。
- **并行策略**：本模块运行在 CPU 主线程，计算密集环节（特征匹配与三角化）通过调用 `cuda_FeatureMatch` 和 `cudaTriangulation` 交由 GPU 并行执行，具体并行粒度由对应 CUDA 模块内部决定（参见后续各模块文档）。
- **关键依赖链**：`runSFM` 直接依赖 `FeatureExtractor`（特征）、`FeatureMatch.cuh`（匹配）、`EssentialMatrix`（本质矩阵）、`PoseRecovery`（姿态）、`Triangulation.cuh`（三角化）、`util`（坐标转换与筛选）、`SFMTypes`（结果结构体）。

【模块】工具函数（特征点筛选）
- **文件**：`util.cpp` / `util.h`
- **职责**：根据掩码筛选特征点。
- **核心接口**：
  - `std::vector<cv::Point2f> fliterInlierPoints(const std::vector<cv::Point2f>& points, const std::vector<uchar>& Mask)`
  - **输入**：`points`（原始点集），`Mask`（长度与 `points` 相同的掩码，非零保留）。
  - **输出**：筛选后的点集（顺序同掩码非零顺序）。
- **数据流**：
  - 在 `SFM.cpp` 中调用两次：第一次传入 `points1`/`points2` 与 `essentialMask`，第二次传入前次结果与 `poseMask`。
  - 筛选结果直接传给 `cudaTriangulation`。
- **实现细节**：遍历 `Mask`，若值非零则取 `points` 同索引元素压入新向量；未做长度检查。


【模块】GPU 三角化（Triangulation）
- **文件**：`Triangulation.cu` / `Triangulation.cuh`
- **职责**：接收二次筛选后的匹配点对及相机内外参，使用 CUDA 核函数执行三角化计算，生成三维点云并过滤无效点。
- **核心接口**：
  - `std::vector<cv::Point3f> cudaTriangulation(const std::vector<cv::Point2f>& inlierPoints1, const std::vector<cv::Point2f>& inlierPoints2, const CameraIntrinsics& intr, const cv::Mat& R, const cv::Mat& t, std::vector<int> Mask3D)`
  - **输入**：`inlierPoints1`/`inlierPoints2`（已二次筛选的匹配点对，来自 `SFM.cpp` 中两次 `fliterInlierPoints` 调用），`intr`（相机内参），`R`（旋转矩阵，3x3），`t`（平移向量，3x1），`Mask3D`（传值，未在函数内部使用，仅作为参数占位）。
  - **输出**：`std::vector<cv::Point3f>` —— 三角化成功且通过深度验证的三维点（世界坐标系）。
- **内部流程**：
  1. **构造投影矩阵**：
     - `P1`：左视图投影矩阵，形式为 `[I | 0]`（3x4），左乘 `intr.K` 得到 3x4 矩阵。
     - `P2`：右视图投影矩阵，左 3x3 填 `R`，第 4 列填 `t`，左乘 `intr.K`。
  2. **矩阵展平**：`flattenMatrix` 将 3x4 矩阵按行优先展开为 `std::array<double, 12>`。
  3. **CPU→GPU 数据传输**：分配显存并拷贝 `P1_flat`、`P2_flat`、`inlierPoints1`、`inlierPoints2` 至设备端。
  4. **核函数调用**：`triangulateKernel<<<gridSize, blockSize>>>`，其中 `blockSize = 256`，`gridSize = (numPoints + blockSize - 1) / blockSize`；传入参数包括左右点集、两个投影矩阵、点数、输出点云地址、深度阈值（`1.5`）、掩码地址。
  5. **GPU→CPU 回传**：拷贝 `d_points3D`（float 数组，每 3 个值一个点）和 `d_Mask3D`（int 数组，非零表示有效点）至主机端向量。
  6. **后过滤**：遍历 `h_Mask3D`，仅保留值非零的点，返回 `finalPoints_3d`。
- **数据流**：
  - **输入来源**：`inlierPoints1`/`inlierPoints2` 来自 `SFM.cpp` 中经 `essentialMask` 和 `poseMask` 两次筛选后的点集；`R`/`t` 来自 `PoseRecovery.cpp` 的 `recoverCameraPose` 输出。
  - **输出去向**：返回值直接赋值给 `SFM.cpp` 中 `Points3D`，用于构造 `result.pointCloud`。
- **并行策略**：每个线程处理一个特征点对（`numPoints` 个点对），由 `gridSize * blockSize` 覆盖全部点；核函数内部执行基于投影矩阵的线性三角化，并结合深度阈值（1.5）与重投影误差进行有效性验证，结果通过 `d_Mask3D` 返回。
- **内存管理**：使用 `cudaMalloc`/`cudaMemcpy`/`cudaFree`（本文件未显式释放，需外部清理）；投影矩阵用 `double` 精度，点坐标用 `float` 精度。


【模块】姿态恢复（PoseRecovery）
- **文件**：`PoseRecovery.cpp` / `PoseRecovery.h`
- **职责**：从本质矩阵分解出旋转矩阵与平移向量，并通过内点数量阈值验证姿态有效性。
- **核心接口**：
  - `bool recoverCameraPose(const cv::Mat3d& E, const std::vector<cv::Point2f>& points1, const std::vector<cv::Point2f>& points2, const CameraIntrinsics& intr, const std::vector<uchar>& essentialMask, cv::Mat& R, cv::Mat& t, int& inlierCount, std::vector<uchar>& poseMask)`
  - **输入**：
    - `E`：本质矩阵（3x3，`cv::Mat3d`），来自 `estimateEssentialMatrix`。
    - `points1`、`points2`：原始匹配点对（来自 `SFM.cpp` 中 `convertMatches` 输出的两视图点集），未经过任何筛选。
    - `intr`：相机内参（包含 `K` 矩阵）。
    - `essentialMask`：极线约束掩码（`std::vector<uchar>`，长度与 `points1` 相同，非零为内点），来自 `estimateEssentialMatrix`。
  - **输出参数**：
    - `R`：旋转矩阵（`cv::Mat`，3x3，引用传出）。
    - `t`：平移向量（`cv::Mat`，3x1，引用传出）。
    - `inlierCount`：姿态解算后的内点数量（`int&`）。
    - `poseMask`：姿态恢复掩码（`std::vector<uchar>&`），长度等于筛选后的内点数量，非零表示通过姿态验证的内点。
  - **返回值**：`bool` —— `true` 表示姿态有效（`inlierCount > 15`），`false` 表示失败。
- **内部流程**：
  1. 调用 `fliterInlierPoints(points1, essentialMask)` 和 `fliterInlierPoints(points2, essentialMask)`（来自 `util.h`），根据 `essentialMask` 筛选出极线内点 `inlierPoints1`、`inlierPoints2`。
  2. 调用 `cv::recoverPose(E, inlierPoints1, inlierPoints2, intr.K, R, t, poseMask)`，将 `R`、`t`、`poseMask` 通过引用传出，返回值 `inlierCount`。
  3. 若 `inlierCount <= 15`，输出错误信息并返回 `false`；否则输出内点数量并返回 `true`。
- **数据流**：
  - **输入来源**：`E` 和 `essentialMask` 来自 `EssentialMatrix.cpp` 的 `estimateEssentialMatrix`；`points1`/`points2` 来自 `SFM.cpp` 的 `convertMatches`。
  - **输出去向**：`R` 和 `t` 直接传给 `cudaTriangulation`；`poseMask` 返回至 `SFM.cpp` 用于第二次 `fliterInlierPoints` 筛选。
- **实现注意**：
  - 内部先通过 `essentialMask` 筛选一次，再将筛选后的点传给 OpenCV 函数；`poseMask` 的长度对应筛选后的点集，而非原始 `points1` 长度。
  - 阈值 `inlierCount > 15` 为硬编码，失败时函数返回 `false` 且不修改 `R`、`t`、`poseMask` 的有效性。

【模块】GPU 特征匹配（FeatureMatch）
- **文件**：`FeatureMatch.cu` / `FeatureMatch.cuh`
- **职责**：在 GPU 上执行左右视图特征描述子的暴力匹配（Brute-Force），并筛选有效匹配；提供转换函数将匹配结果转为 OpenCV 点坐标。
- **核心接口**：
  - `std::vector<cuda_MatchResult> cuda_FeatureMatch(const FeatureSet& f1, const FeatureSet& f2)`
    - **输入**：`f1`、`f2`（`FeatureSet` 类型，包含 `numFeatures`、`descriptors`（`std::vector<float>`，长度为 `numFeatures * 128`）、`cvKeypoints`（`std::vector<cv::KeyPoint>`））。
    - **输出**：`std::vector<cuda_MatchResult>`，长度等于 `f1.numFeatures`，每个元素包含 `bestIdx`（`int`，最佳匹配右图特征索引，`-1` 表示无有效匹配）和 `secondBestIdx`（结构体中未显示，但推测由核函数填充）。
  - `void convertMatches(const FeatureSet& f1, const FeatureSet& f2, const std::vector<cuda_MatchResult>& cuda_result, std::vector<cv::Point2f>& points1, std::vector<cv::Point2f>& points2)`
    - **输入**：`f1`、`f2`、`cuda_result`（`cuda_FeatureMatch` 的返回值）。
    - **输出**：`points1`、`points2`（引用传出），分别为左图、右图中匹配点的像素坐标，仅保留 `cuda_result[i].bestIdx >= 0` 的对应点。
- **数据流**：
  - **输入来源**：`f1`、`f2` 来自 `SFM.cpp` 中 `extractFeaturesFromImages()` 返回的 `features[0]` 和 `features[1]`。
  - **内部处理**：描述子数据拷贝至 GPU 显存 → 调用 `matchBruteForce` 核函数（块大小 256，网格大小由 `f1.numFeatures` 计算）进行 L2 距离比对与比值测试（阈值 0.75f），结果写入 `d_result` → 回传至主机端 `h_result`。
  - **输出去向**：`cuda_FeatureMatch` 返回的 `h_result` 直接传给 `convertMatches`（在 `SFM.cpp` 中调用），`convertMatches` 产出的 `points1`、`points2` 传递给 `estimateEssentialMatrix`。
- **辅助函数**：
  - `int countValidFeature(const std::vector<cuda_MatchResult>& h_result)`：统计 `bestIdx >= 0` 的匹配数量，用于输出日志（`[SFM] img0 - img1: count matches`）。
- **实现细节**：
  - 描述子维度固定为 128（SIFT）。
  - 核函数 `matchBruteForce` 的第四个参数 `0.75f` 为比值测试阈值。
  - `convertMatches` 中遍历 `i` 从 0 到 `f1.numFeatures - 1`，若 `j = cuda_result[i].bestIdx >= 0`，则分别取 `f1.cvKeypoints[i].pt` 和 `f2.cvKeypoints[j].pt` 压入 `points1`、`points2`。
  - 显存分配与释放均在函数内完成（`cudaMalloc`/`cudaFree`）。

【模块】特征提取（FeatureExtractor）
- **文件**：`FeatureExtractor.cpp` / `FeatureExtractor.h`
- **职责**：从固定路径读取多张图像，对每张图像提取 SIFT 特征，返回特征集列表。
- **核心接口**：
  - `std::vector<FeatureSet> extractFeaturesFromImages()`
    - **输入**：无参数。内部调用 `robustImreadIndexed(int index)` 尝试多个路径前缀（`"data/img" + std::to_string(index) + ".jpg"`、`"../" + ...`、`"../../" + ...`、`"../../../" + ...`）读取图像，从 `index = 1` 开始递增，直至某张读取失败时停止。
    - **输出**：`std::vector<FeatureSet>`，每个元素对应一张成功读取的图像，顺序为图像索引递增（1,2,3...）。若图像数量 `< 2`，输出错误信息并返回空向量。
  - 内部辅助函数 `extractSIFTFeatures(const cv::Mat& image)`（未在头文件中声明，为静态使用）：
    - **输入**：`cv::Mat` 图像（彩色或灰度）。
    - **输出**：`FeatureSet`，包含 `numFeatures`（特征点数量）、`kpX`/`kpY`（`std::vector<float>`，特征点 x/y 坐标）、`descriptors`（`std::vector<float>`，展平的描述子，长度为 `numFeatures * 128`）、`cvKeypoints`（`std::vector<cv::KeyPoint>`）。
- **数据流**：
  - **输入来源**：图像文件（硬编码文件名 `"data/imgX.jpg"`，X 为从 1 开始的整数）。
  - **内部处理**：
    1. `extractFeaturesFromImages` 循环调用 `robustImreadIndexed` 读图，存入 `images` 向量。
    2. 对每张图调用 `extractSIFTFeatures`：转灰度 → 创建 `cv::SIFT` 对象 → `detectAndCompute` 提取关键点与描述子（`CV_32F` 类型，列数固定为 128）→ 填充 `FeatureSet` 成员（`kpX`/`kpY` 从 `cv::KeyPoint::pt` 取出，`descriptors` 从 `descriptorsMat` 内存拷贝，`cvKeypoints` 移动赋值）。
  - **输出去向**：返回值传给 `SFM.cpp` 中 `runSFM` 的 `features` 变量，供 `cuda_FeatureMatch` 使用。
- **实现细节**：
  - 若输入图像为彩色（`channels() == 3`），转灰度；否则直接使用。
  - 若 `numFeatures == 0`，输出警告并返回空 `FeatureSet`。
  - 使用 `CV_Assert` 校验描述子类型、列数（`FeatureSet::DESC_DIM`）及连续性（`isContinuous()`）。
  - `robustImreadIndexed` 为静态函数，仅在当前文件内可见。

【模块】本质矩阵估计（EssentialMatrix）
- **文件**：`EssentialMatrix.cpp` / `EssentialMatrix.h`
- **职责**：基于匹配点对与相机内参，使用 RANSAC 估计本质矩阵，并输出极线约束内点掩码。
- **核心接口**：
  - `cv::Mat estimateEssentialMatrix(const std::vector<cv::Point2f>& points1, const std::vector<cv::Point2f>& points2, const CameraIntrinsics& intr, std::vector<uchar>& essentialMask)`
  - **输入**：
    - `points1`、`points2`：匹配点对像素坐标（来自 `convertMatches` 的输出）。
    - `intr`：相机内参（包含 `K` 矩阵）。
  - **输出参数**：
    - `essentialMask`（`std::vector<uchar>&`）：长度与 `points1` 相同，非零表示通过极线约束的内点，引用传出。
  - **返回值**：`cv::Mat` —— 3x3 本质矩阵 `E`。
- **内部流程**：
  - 调用 `cv::findEssentialMat(points1, points2, intr.K, cv::RANSAC, 0.999, 1.5, essentialMask)`。
  - 参数 `0.999` 为 RANSAC 置信度，`1.5` 为投影误差阈值（像素单位），`essentialMask` 由 OpenCV 填充。
- **数据流**：
  - **输入来源**：`points1`、`points2` 由 `SFM.cpp` 中 `convertMatches` 产出。
  - **输出去向**：返回值 `E` 传给 `PoseRecovery.cpp` 的 `recoverCameraPose`；`essentialMask` 在 `SFM.cpp` 中用于第一次 `fliterInlierPoints` 筛选。
- **实现细节**：本函数为 `cv::findEssentialMat` 的薄封装，不包含额外几何计算。


【模块】相机内参（CameraIntrinsics）
- **文件**：`CameraIntrinsics.cpp` / `CameraIntrinsics.h`
- **职责**：定义相机内参结构体，提供内参创建与像素坐标归一化工具函数。
- **核心数据结构**：
  - `struct CameraIntrinsics`
    - 成员：`fx`、`fy`、`cx`、`cy`（`float` 类型）。
    - `K`（`cv::Mat3f`）：3x3 内参矩阵。
    - `Kinv`（`cv::Mat3f`）：K 的逆矩阵。
- **核心接口**：
  - `CameraIntrinsics createCameraIntrinsics(float fx, float fy, float cx, float cy)`
    - **输入**：四个内参参数（焦距 `fx`、`fy`，主点 `cx`、`cy`）。
    - **输出**：`CameraIntrinsics` 结构体，`K` 被赋值为 `[fx, 0, cx; 0, fy, cy; 0, 0, 1]`（使用 `cv::Mat_<double>` 构造，但结构体成员 `K` 声明为 `cv::Mat3f`，类型存在隐式转换），`Kinv` 赋值为 `K.inv()`。
  - `cv::Vec2f pixelToNormalized(const CameraIntrinsics& intr, float px, float py)`
    - **输入**：`intr`（相机内参），`px`、`py`（像素坐标）。
    - **输出**：`cv::Vec2f(xn, yn)`，其中 `xn = (px - intr.cx) / intr.fx`，`yn = (py - intr.cy) / intr.fy`（归一化相机坐标，无畸变模型）。
- **数据流**：
  - **输入来源**：`createCameraIntrinsics` 的参数由外部调用方提供（未在本项目源码中体现具体数值）。
  - **输出去向**：`CameraIntrinsics` 对象作为参数传入 `SFM.cpp` 的 `runSFM`，并传递给 `estimateEssentialMatrix`、`recoverCameraPose`、`cudaTriangulation` 等下游模块。
- **实现细节**：
  - 结构体注释标注“暂无畸变”。
  - `pixelToNormalized` 使用 `float` 类型运算，返回 `cv::Vec2f`。
  - `K` 和 `Kinv` 类型为 `cv::Mat3f`，但 `createCameraIntrinsics` 中使用 `cv::Mat_<double>` 构造，OpenCV 支持赋值转换。

【模块】CUDA 数据结构定义（d_FeatureSet）
- **文件**：`d_FeatureSet.cuh`
- **职责**：定义 GPU 端特征集与匹配结果的结构体。
- **数据结构**：
  - `struct cuda_FeatureSet`
    - 成员：`int numFeatures`（特征数量），`float* descriptors`（设备端描述子指针，初始化为 `nullptr`）。
  - `struct cuda_MatchResult`
    - 成员：`int bestIdx`（最佳匹配右图特征索引），`float bestDist`（最佳匹配距离）。


【模块】CUDA 特征匹配核函数（matchBruteForce）
- **文件**：`FeatureMatch.cu` / `FeatureMatch.cuh`
- **职责**：在 GPU 上执行暴力匹配核函数，计算 L2 距离并执行比值测试。
- **核函数接口**：
  - `__global__ void matchBruteForce(const float* descriptors1, int numFeature1, const float* descriptors2, int numFeature2, float ratioThresh, cuda_MatchResult* results)`
  - **输入**：`descriptors1`（左图描述子，设备指针，长度为 `numFeature1 * 128`），`numFeature1`，`descriptors2`（右图描述子，长度为 `numFeature2 * 128`），`numFeature2`，`ratioThresh`（比值测试阈值，传入 `0.75f`）。
  - **输出**：`results`（设备指针，长度 `numFeature1`，每个元素填充 `bestIdx` 和 `bestDist`）。
- **内部逻辑**：
  - 每个线程处理一个左图特征点（索引 `i = blockIdx.x * blockDim.x + threadIdx.x`，`i >= numFeature1` 则返回）。
  - 调用 `squaredL2Distance`（设备函数，计算 128 维向量平方 L2 距离）遍历所有右图描述子，记录 `bestDist` 与 `secondDist`。
  - 比值测试：若 `bestDist < ratioThresh^2 * secondDist`，则 `results[i].bestIdx = bestIdx`，`results[i].bestDist = bestDist`；否则 `bestIdx = -1`，`bestDist = 0.0f`。
- **数据流**：
  - **输入来源**：`descriptors1`、`descriptors2` 由 `cuda_FeatureMatch` 函数从主机端 `FeatureSet` 拷贝至 GPU。
  - **输出去向**：`results` 由 `cuda_FeatureMatch` 拷贝回主机端 `std::vector<cuda_MatchResult>`。


【模块】CUDA 三角化核函数（triangulateKernel）
- **文件**：`TriangulationCUDA.cu` / `TriangulationCUDA.cuh`
- **职责**：对每个匹配点对执行线性三角化，进行深度检测与重投影误差验证，输出有效三维点。
- **核函数接口**：
  - `__global__ void triangulateKernel(const float* points1, const float* points2, double* P1, double* P2, int numPoints, float* points3D, const float reprojThreshold, int* Mask)`
  - **输入**：
    - `points1`、`points2`：设备端点坐标（`float` 数组，每点占 2 个 `float`，`points1[2*i]`、`points1[2*i+1]` 为第 i 点）。
    - `P1`、`P2`：设备端投影矩阵（`double` 数组，按行优先展平，长度 12）。
    - `numPoints`：点数。
    - `reprojThreshold`：重投影误差阈值（传入 `1.5`）。
  - **输出**：
    - `points3D`：设备端三维点（`float` 数组，每点占 3 个 `float`）。
    - `Mask`：设备端掩码（`int` 数组，非零表示有效点）。
- **内部逻辑**：
  1. 每个线程处理一个点对（`i = blockIdx.x * blockDim.x + threadIdx.x`，`i >= numPoints` 则返回）。
  2. 构造 4x4 矩阵 `A`（基于投影矩阵与像素坐标的线性三角化方程）。
  3. 调用 `solveAX0(A, X)`（设备函数，求解 `AX = 0` 的最小二乘解）得到齐次坐标 `X`。
  4. 归一化得 `(x, y, z, w)`，若 `w < 1e-12` 或 `z <= 0` 或 `z2 <= 0`（右图深度），则 `Mask[i] = 0` 并返回。
  5. 计算两视图重投影误差 `maxErr`（取两个误差最大值），若 `maxErr < reprojThreshold`，则写入 `points3D[3*i] = (float)x` 等，`Mask[i] = 1`；否则 `Mask[i] = 0`。
- **数据流**：
  - **输入来源**：`points1`、`points2`、`P1`、`P2` 由 `cudaTriangulation` 函数从主机端拷贝至 GPU。
  - **输出去向**：`points3D`、`Mask` 由 `cudaTriangulation` 拷贝回主机端 `std::vector<cv::Point3f>` 与 `std::vector<int>`。


【模块】CUDA 设备端工具函数（util.cu / util.cuh）
- **文件**：`util.cu` / `util.cuh`
- **职责**：提供核函数中调用的数学工具函数。
- **函数接口**：
  - `__device__ bool solveAX0(const double A[4][4], double X[4])`
    - **输入**：`A`（4x4 矩阵）。
    - **输出**：`X`（4 维向量，`AX = 0` 的最小二乘解，归一化后传出）。
    - **实现**：通过雅可比迭代（最大迭代 100 次，收敛阈值 `eps = 1e-12`）对 `B = A^T A` 进行特征分解，取最小特征值对应的特征向量作为解，返回 `true` 表示求解成功，`false` 表示失败（范数 `< 1e-15`）。
  - `__device__ float2 normalizePoints(float u, float v, float fx, float fy, float cx, float cy)`
    - **输入**：像素坐标 `(u, v)`，内参 `fx`、`fy`、`cx`、`cy`。
    - **输出**：`float2(xn, yn)`，其中 `xn = (u - cx) / fx`，`yn = (v - cy) / fy`。
- **数据流**：被 `triangulateKernel` 调用（当前 `util.cuh` 中声明，但 `triangulateKernel` 未直接调用 `normalizePoints`，而是独立实现了三角化；`solveAX0` 被 `triangulateKernel` 调用）。


【模块】CUDA 本质矩阵验证（未完成）
- **文件**：`findEssentialMat.cu` / `findEssentialMat.cuh`
- **状态**：文件内容为注释代码，未实现功能。
- **文件内容**：
  - `findEssentialMat.cu` 中包含注释掉的核函数 `validateEssentialMatrixCUDA`，声明用于 GPU 验证本质矩阵。
  - `findEssentialMat.cuh` 中注释掉函数声明。
  - 当前无实际调用或实现逻辑。